language: php
php:
  - 7.0
  - 5.6
  - 5.5
  - nightly
  - hhvm

matrix:
  allow_failures:
    - php: nightly

sudo: false

before_script:
  - composer selfupdate
  - composer update --no-interaction
  - composer require --dev "codeclimate/php-test-reporter:dev-master"

script:
  - ./vendor/phpunit/phpunit/phpunit -v --coverage-clover build/logs/clover.xml

addons:
    code_climate:
        repo_token: 13afd1f85dd51916797a85c6e4141e665ce4ea6db0c51fb299cfc302ecb67951